# AGENTS.md — TrafficGraph Codex 项目指令

> 本文件是 Codex CLI 的入口；完整工程规范以 `CLAUDE.md` 为准，
> 当前实现状态以 `AI_CONTEXT.md` 为准。

## 启动检查

1. 阅读 `CLAUDE.md`。
2. 阅读 `AI_CONTEXT.md`。
3. 所有新增代码、配置、文档和文件名必须保持交通领域命名。
4. 完成后运行 `python -m pytest src/tests -q`。
